---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import '../styles/global.css';

interface Props {
  title: string;
  description: string;
  wide?: boolean;
  image?: string;
  home?: boolean;
}

const { title, description, wide = false, image = '/og-image.png', home = false } = Astro.props;
const lang = (Astro.currentLocale ?? 'en') as 'en' | 'ru';
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const ogImage = new URL(image, Astro.site);
const homeTitle = { en: 'Kolosei — AI-powered Hebrew language tools', ru: 'Kolosei — AI-инструменты для иврита' }[lang];
const fullTitle = home ? homeTitle : `${title} — Kolosei`;
// hreflang alternates (path stripped of the /ru locale prefix)
const pathNoSlash = Astro.url.pathname.replace(/\/$/, '') || '/';
const basePath = pathNoSlash.startsWith('/ru') ? (pathNoSlash.slice(3) || '/') : pathNoSlash;
const enHref = new URL(basePath, Astro.site);
const ruHref = new URL(basePath === '/' ? '/ru' : '/ru' + basePath, Astro.site);
---
<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />

    <title>{fullTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />
    <link rel="alternate" hreflang="en" href={enHref} />
    <link rel="alternate" hreflang="ru" href={ruHref} />
    <link rel="alternate" hreflang="x-default" href={enHref} />

    <meta property="og:type" content="website" />
    <meta property="og:locale" content={lang === 'ru' ? 'ru_RU' : 'en_US'} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:site_name" content="Kolosei" />
    <meta property="og:image" content={ogImage} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={fullTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />

    <script type="application/ld+json" set:html={JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'Organization',
      name: 'Kolosei',
      url: 'https://kolosei.com',
      logo: 'https://kolosei.com/favicon.svg',
      image: 'https://kolosei.com/og-image.png',
      email: 'peter@kolosei.com',
      description: 'Kolosei is a founder-led studio building AI-powered Hebrew language tools, with LinguistPro as its flagship. Built on Google Cloud.',
      address: {
        '@type': 'PostalAddress',
        addressLocality: 'Kiryat Bialik',
        addressCountry: 'IL',
      },
      sameAs: [
        'https://github.com/SindromRadioSpb',
        'https://osf.io/zdv9j',
      ],
      founder: {
        '@type': 'Person',
        name: 'Peter Kolosei',
        sameAs: ['https://github.com/SindromRadioSpb'],
      },
      knowsAbout: [
        'Hebrew language learning',
        'Natural language processing',
        'Hebrew lexicography',
        'Google Cloud',
        'Gemini API',
        'Rare-disease genomics',
      ],
    })} />
  </head>
  <body>
    <Header />
    <main class={wide ? 'wide' : ''}>
      <slot />
    </main>
    <Footer />
  </body>
</html>
